{
  "name": "incident-ai-server",
  "version": "1.0.0",
  "description": "Thin local server holding the Azure AI Foundry credentials for the one live-toggle scenario. Every other path in the demo uses pre-computed cached responses.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env --use-system-ca index.js"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^5.2.1"
  }
}

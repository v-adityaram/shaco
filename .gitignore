node_modules/
dist/
.env
.env.local
*.log
__pycache__/
*.pyc
.claude/

# regenerable raw generator/normaliser output (run generator/generate.py +
# normaliser/normalise.py to recreate) -- the app only needs app/src/data
/data/

# raw ops exports contain employee names / ticket text -- never commit
/Auto Alerts/
/server/cache/

# redacted-but-still-real ticket text extracted from /Auto Alerts/ for the
# live-mode demo -- names are stripped but subjects/close-notes are real
# client text, so this stays local and gitignored like the source export
/server/live-data/

# generated architecture page (regenerate: python scripts/build_architecture_html.py)
/docs/architecture.html
